div(id=contentId class="three wide column")
  figure(id=contentId class="hand hand-spread")
    img(src=image)
    img(src=image)
    img(src=image)
    img(src=image)
    img(src=image)
    script.
      function afterAddDomContent() {
          $('#modal_#{contentId}')
            .modal('setting', 'transition', 'horizontal flip')
            .modal('setting', 'detachable', 'false');
          $('##{contentId}').on('click', function() {
            $('#modal_#{contentId}')
              .modal('refresh')
              .modal('show');
          });
      }
  div(id="modal_" + contentId class="ui centered basic modal")
    div(class="ui cards")
      each card in cards
        if (cards.length <= 3)
          include ./card_hand_content.pug
        else
          include ./card_hand_content_mini.pug
    div(class="actions")
      div(class="ui green red ok inverted button")
        i(class="remove icon")
        |schliessen
