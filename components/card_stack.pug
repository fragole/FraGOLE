div(id=contentId style="position:absolute; top:" + y +"px; left:"+ x +"px;")
  .ui.header #{label}
  figure(id=contentId + "_inner" class="stack stack-spread" )
    img(src=image)
    img(src=image)
    img(src=image)
    img(src=image)
    img(src=image)
  script
    |function afterAddDomContent() {
    if (highlight == 'on')
        |    $('##{contentId}_inner' )
        |        .transition('set looping')
        |        .transition('jiggle', '2000ms');
    if (highlight == 'off')
        |    $('##{contentId}_inner').transition('remove looping');
    if (activate == 'on')
        |    $('##{contentId}_inner').on('click', function() {rpcServer['click_#{id}'](gameboard.clientId)});
    if (activate == 'off')
        |    $('##{contentId}_inner').off('click');
    |}
